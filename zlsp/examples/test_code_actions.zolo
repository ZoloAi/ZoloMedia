zMeta:
  Data_Type: test

# Test 1: Type hints (should trigger "Add Type Hint" action)
users:
  name: john_doe
  email: john@example.com
  age: 25
  active: true
  score: 98.5

# Test 2: Indentation issues (should trigger "Fix Indentation" action)
config:
  host: localhost
    port: 8080

# Test 3: zSchema fields (should trigger "Add Required Fields" for zSchema files)
# (This works best in a zSchema.*.zolo file, see below)
